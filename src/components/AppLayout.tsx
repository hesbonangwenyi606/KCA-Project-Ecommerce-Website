import React from 'react';
import Home from '@/pages/Home';

// App routing is handled in App.tsx. This layout renders the storefront home page.
const AppLayout: React.FC = () => {
  return <Home />;
};

export default AppLayout;
